class Trip {
  final String title;
  final DateTime startDate;
  final DateTime endDate;
  final double budget;
  final String travelType;
  Trip(this.title,this.budget,this.endDate,this.startDate,this.travelType);
}